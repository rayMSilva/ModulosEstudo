const name = 'ray'; //declarando uma variável constante de compilação que só pode ser declarada apenas uma vez durante execução da aplicação



void main()
{
  print(name.toUpperCase());
  String newNome = name + ' Michel';
  print(newNome);
}